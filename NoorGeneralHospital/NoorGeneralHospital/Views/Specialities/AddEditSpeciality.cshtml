@model NoorGeneralHospital.Models.Speciality

@{
    ViewBag.Title = "Create/Update";
}
<div class="row">
    <div class="col-lg-8 offset-lg-2">
        <h4 class="page-title">Create/Update Speciality</h4>
    </div>
</div>
<div class="row">
    <div class="col-lg-8 offset-lg-2">
        @using (Html.BeginForm("SaveSpeciality", "Specialities", FormMethod.Post, new { enctype = "multpart/form-data" }))
        {
            @Html.HiddenFor(s => s.Id)
            <div class="form-group">
                <label>Speciality Name</label>
                @Html.TextBoxFor(s => s.SpecialityName, new { @class = "form-control", @placeholder = "Enter Speciality" })
                @Html.ValidationMessageFor(s => s.SpecialityName, null, new { @class = "text-danger" })
            </div>
            <div class="form-group">
                <label class="display-block">Status</label>
                <div class="form-check form-check-inline">
                    @Html.RadioButtonFor(s => s.IsActive, true, new { @class = "form-check-input", @name = "status", @id = "speciality_active", @checked= "checked"})
                    <label class="form-check-label" for="speciality_active">
                        Active
                    </label>
                </div>
                <div class="form-check form-check-inline">
                    @Html.RadioButtonFor(s => s.IsActive, false, new { @class = "form-check-input", @name = "status", @id = "speciality_inactive" })
                    <label class="form-check-label" for="speciality_inactive">
                        Inactive
                    </label>
                </div>
            </div>
            <div class="m-t-20 text-center">
                <button class="btn btn-primary submit-btn">Create</button>
            </div>
        }
    </div>
</div>
<script src="~/Content/assets/js/jquery.validate.min.js"></script>
<script src="~/Content/assets/js/jquery.validate.unobtrusive.min.js"></script>
<script>
    $(document).ready(function () {

        $('form').submit(function (e) {
            debugger
            e.preventDefault();
            var isValid = $(this).validate().form();
            if (!isValid) return false;
            else {
                $.ajax({
                    url: '/Specialities/SaveSpeciality',
                    data: $(this).serialize(),
                    type: 'POST',
                    beforeSend: function () {
                    },
                    complete: function () {
                    },
                    success: function (res) {
                        if (res.Code == "1") {
                            toastr.success(res.Message, 'success!');
                            _SpecialityDetail();
                        }
                        else if (res.Code == "2")
                        {
                            toastr.warning(res.Message, 'warning!');
                        }
                        else {
                            toastr.error(res.Message, 'error!');
                        }
                    },
                    error: function (jqXHR, error, errorThrown) {
                        toastr.error('Sorry Try again', 'error!');
                    },
                });
            }
        });

    });</script>